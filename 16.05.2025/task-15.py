from itertools import combinations

D = frozenset(range(7,68))
C = frozenset(range(2,100))

def f(A1,A2):
    A = A1 <= x A2
    return (x in D)<=((not(x in C) and not(x in A)) <= (x in D))
ans = []


